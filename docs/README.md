# docs/

Static media referenced from the top-level README and `project-description.md`.

## Expected files

| file | purpose | referenced from |
|---|---|---|
| `verbalize-divergence-demo.gif` | Short loop showing a live misalignment detection: compliant verbal output on the left, residual-stream thought stream on the right, judge lighting up amber when divergence appears. | `README.md`, `project-description.md` |

The GIF will be added before submission. Until then the README and the
project description carry commented-out `<img>` references next to TODO
markers; uncomment them once the file lands here.

## Sizing guidance

- Aim for ≤ 10 MB so GitHub renders it inline.
- 800–1200 px wide; height follows the captured UI region.
- ~6–10 s loop is enough to see one divergence event.

__pycache__/
.venv/
actor_hf/
*.pyc
.DS_Store

# Secrets — NEVER commit. .env.example is the template, .env is local-only.
.env
.env.*
!.env.example

# Frontend build artifacts (frontend/.gitignore covers most, this is belt-and-suspenders)
frontend/.next/
frontend/node_modules/

# Vercel
.vercel/
